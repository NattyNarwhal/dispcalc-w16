I [wrote an article](https://cmpct.info/~calvin/Articles/Win16.htm) about this, which explains.

## Building

Open the makefile in Visual C++, or run (in an MS-DOS prompt with environment variables set up)

```
nmake /f dispcalc.mak dispcalc.exe
```
